@model List<zakjr.Models.BlogPost>
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Blog Admin";
}

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Date</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ID)</td>
                <td>@Html.DisplayFor(modelItem => item.Title)</td>
                <td>@Html.DisplayFor(modelItem => item.PostDate)</td>
                <td><a asp-action="Edit" asp-route-id="@item.ID" class="button">Edit</a></td>
            </tr>
        }
    </tbody>
</table>
